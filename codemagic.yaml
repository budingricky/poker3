workflows:
  debug-ghost-cert:
    name: 🕵️ Ghostbuster Debug
    instance_type: mac_mini_m1
    environment:
      groups:
        - apple_credentials
    scripts:
      - name: List All Certificates
        script: |
          echo "正在连接 Apple 后台..."
          # 列出 API 能看到的所有证书
          app-store-connect certificates list --verbose